apiVersion: v1
kind: Service
metadata:
  name: deutsche-gesetze-mcp-server
spec:
  selector:
    app: deutsche-gesetze-mcp-server
  ports:
    - name: http
      port: 8001
      targetPort: 8001
  type: ClusterIP
